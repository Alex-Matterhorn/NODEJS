const ex_module = require("ex_module");

console.log(ex_module());

// calling module from node_modules without ./ in adress  !!
// node_modules folder can have all youres modules in it to make code more clear !